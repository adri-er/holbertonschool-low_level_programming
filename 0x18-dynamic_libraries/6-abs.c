#include <stdio.h>
#include <stdlib.h>
#include "holberton.h"

/**
 *_abs - Print the sign of a number.
 *@n: The number you want to convert to absolute value.
 *
 *Return: Absolute value of number.
 */
int _abs(int n)
{
	return (abs(n));
}
