#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int bs;

	for (bs = 0; bs <= 9; bs++)
	{
		printf("%d", bs);
	}
	printf("%s", "\n");
	return (0);
}
